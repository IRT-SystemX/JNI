# Instructions to deploy JNI using `docker compose` 

Inside `JNI/deployment/docker`
1. Add a `.env` file (an example is provided in `.env.example`)
2. Run `docker compose up -d --build`

The plateform is accessible at `http://localhost:3000`